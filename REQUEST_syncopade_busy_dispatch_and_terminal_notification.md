# Syncopade改訂依頼: 使用中ノードへの配送・再試行打切り・親への通知

発行日: 2026-09-09  
依頼元: Julia_script_ManipMDO_ICRA2027（SO6後段接続 Step 8）  
依頼先: Julia_PKG_syncopade

## 要旨と依頼範囲

受付済みの1タスクについて、使用中のノードへの配送が4回拒否され、約0.3秒で
再試行上限に達して破棄された。親にはその失敗が届かず、結果待ちが続いた。
同時に、別タスクを受け付けたばかりのノードの管理状態がbusyからidleへ戻る記録がある。

以下の3件を分けて調査・改訂してほしい。**現象は実ログで確認済みだが、
状態を巻き戻した内部処理の特定は未完了**。3件が独立した根本原因とは断定しない。
この文書は実装TODOではなく依頼書。対策案は方向性であり、具体的な同期方式・待機方針・
通信形式はSyncopade側で検討してほしい。依頼元ではpackage改訂・再投入を行っていない。

## 共通の発生条件・証拠

- conductor: `192.168.12.4:9004`、結果を受ける親: `192.168.12.2:9102`。
- 実行ID: `so6-downstream-on-20260909`。
- 受付済みで未返却となったtask ID: `5799dcc7-078f-431d-abe9-f323c5724c82`。
- 親側の識別: `so6-downstream-on-20260909-call-6`の`batch-2`。
  SO5第1軸の第5世代、4候補を各1タスクで投入したうちの1件。
- 関数: `SyncopadeSO5CandidateBatchTask.execute_so5_candidate_batch_task`。
- 対象requestのSHA-256: `1b4ee9370c24083afb48a43ae2faafa6f2d14d0adacef83643b03f8cc0bfd2c2`。
- 親は各タスクを1回だけ投入し、自動再投入なし。今回ログにある3回の再試行は**conductor内部**のもの。
- 登録12台、実行前に利用可能7台を確認。利用可能な全台のcache clear成功を確認してから投入した。
  登録済みで停止中の5台は正常な運用条件であり、全台起動を要求する改訂にはしない。
- 親が使用するSyncopadeはv0.1.3、revision `8dfcc135b9c87c13950757cc344fb398a98fe708`。
  下記のコード照合はこの固定revisionに対して行った。
  **実稼働conductor/workerのGit revisionと未コミット差分は未取得**なので、修正前に別途確認してほしい。
  conductorの例外記録にはWindows側の`Julia_PKG_syncopade`パスが出ている。

正本ログは先生が共有先に複製した
[conductor_events.csv](/Volumes/syncopade_nfs/conductor_events.csv:3531991)。
今回読んだファイルは740,645,131 bytes、SHA-256は
`24a832f322b35197a18fef7a986fc8c8b07c494be670142abb17375d9a0fde2f`。
以下の時刻はすべてこのログの2026-09-09の時刻（JST）。親ログはUTCなので9時間ずれる。
CSV内の例外メッセージには改行がある。以下の行番号はCSVレコード番号でなく物理行番号。

## バグ1: 計算中のノードがconductor上でidleに戻る

### 起きた現象（確認済み）

- 17:19:39.619: 12番（`192.168.12.12:8012`、C-3PX）へ別タスクの配送が成功し、busyになった。
- 17:19:39.650: 11番（`192.168.12.11:8011`、GNK_EG-6）も別タスクを受け付けたが、
  同時刻に11番と12番がともにbusy→idleへ変更された。
- 17:19:39.659: conductorは12番へ問題のタスクを配送し、workerから`ERROR|BUSY`で拒否された。
- 先に受け付けた別タスクは12番が17:19:53.404、11番が17:19:55.247に正常終了している。
  idleへ戻された時点で、そのタスクの終了通知はまだ来ていない。
- 根拠: 正本ログ3531994〜3532007行、末尾の別タスクの`TASK_DONE`。

### 考えられる原因（推測とコード上の手掛かり）

状態確認を開始した時点ではidleだった応答が、別タスクの受付によるbusy更新より後に
反映され、新しい状態を古い状態で上書きしている可能性が高い。

照合した`syncopadeConductor.jl`では、`refresh_states_until_idle!`（571行付近）と
`monitor_nodes`（699行付近）が並列取得した状態を順に`set_node_state!`へ反映する。
`dispatch_to_worker`は受付後にbusyへ更新する（626行付近）。配送処理のlockは存在するが、
状態確認の取得・適用全体はそのlockで一体には守られていない。
個々の辞書更新にlockがあっても、「どちらの観測が新しいか」は保証されない構成に見える。
ただし、今回どの状態更新経路が上書きしたかは、このログだけでは確定できない。

### 対策案

- 配送中/受付済みの割当てと、後から戻る状態確認の整合を保証する。
  例として、割当ての世代番号・観測時点を照合する、古い応答を適用しない、
  割当ての状態遷移を一箇所に集めるなどを検討してほしい。具体方式は指定しない。
- idleへ解放するときも、どの割当て・終了通知に対応するかを確認し、古い終了通知で
  新しい仕事を実行中のノードを解放しない。これは関連する回帰確認事項であり、今回の観測事実ではない。
- workerの使用中拒否・同一ノードでの実行排他は維持する。今回の`BUSY`応答自体は防御として正しい。

確認してほしい小テスト: 状態確認のidle応答を意図的に遅らせ、その間に別タスクを受け付けてbusyへ
変更する。遅れたidle応答が届いても、実行中の割当てがidleへ巻き戻らないことを確認する。
ランダムな大量投入だけでなく、更新順を制御して再現できる試験を希望する。

## バグ2: BUSYを配送故障として扱い、即時再試行で仕事を破棄する

### 起きた現象（確認済み）

同じtask IDの履歴は次のとおり。すべての拒否理由は`ERROR|BUSY`だった。

| 試行 | 配送開始 | 配送先 | 拒否を記録した時刻 | その後 |
| --- | --- | --- | --- | --- |
| 初回 | 17:19:39.659 | 12番 | 17:19:39.720 | retry=1として再待機 |
| 再試行1 | 17:19:39.720 | 12番 | 17:19:39.754 | retry=2として再待機 |
| 再試行2 | 17:19:39.762 | 11番 | 17:19:39.919 | retry=3として再待機 |
| 再試行3 | 17:19:39.925 | 12番 | 17:19:39.950 | `TASK_DROPPED`、`max_retry_exceeded` |

初回の配送開始から破棄まで0.291秒。ノードが空く前に再試行回数を使い切った。
拒否に伴ってconductorのノード状態がdownへ変更される記録もあるが、両ノードは
先に受け付けた別タスクを正常に完了している。今回の拒否はノード故障や数値計算失敗ではない。
根拠: 3532005〜3532055行（例外本文3532007、3532020、3532034、3532047行）。

### 考えられる原因

照合したコードでは`syncopade_calc_request`が`ERROR|BUSY`を一般の例外にし、
`dispatch_to_worker`のcatchが一律にノードをdownへ変更してfalseを返す（638行付近）。
続く配送ループは失敗種別を分けず`requeue_with_retry!`を呼び（668行付近）、
回数を増やして再び配送対象にする。バグ1によるidleへの巻き戻りと組み合わさり、
同じ使用中ノードへ短時間に再配送されたと考えられる。

### 対策案

- 「使用中なので未受理」と、通信障害・受付成否不明・実行失敗を区別する。
  BUSYならノードを故障扱いせず、仕事を保持して空きノードを待つ方向を検討してほしい。
- 使用中拒否だけで通常の故障再試行枠を瞬時に消費しないようにする。
  空き状態の再確認、再配送の間隔、全体待機期限などの具体方針はSyncopade側で決めてほしい。
- 台数固定・全台起動を前提にしない。一方で無期限に待たせるだけにもせず、
  最終的に打ち切る場合はバグ3の終端通知へつなぐ。
- `BUSY`で未受理と分かる場合と、受付応答のtimeoutで実行開始の有無が分からない場合を混同しない。
  後者に無条件再投入を適用して二重実行を起こさない。

確認してほしい小テスト: 使用中を複数回返してから空くworkerで、タスクが消えず、
空いた後に1回だけ実行されることを確認する。利用可能1台・他の登録ノード停止でも成立させる。
長時間空かない場合は、定めた期限と終端通知が機能することを別に確認する。

## バグ3: 受付済みタスクを破棄しても親へ終端通知が来ない

### 起きた現象（確認済み）

- 親は問題のtask IDを受付結果として受け取った。
- conductorは17:19:39.950にそのタスクの`TASK_DROPPED`を記録した。
- 同じ世代の他3件は約14〜16秒で正常に返却されたが、この1件の結果・失敗通知は親に届かなかった。
  対応するworker出力directoryも生成されていない。
- 親は全体待機期限1800秒を持つが、17:24:56の時点でも未返却で、利用可能7台は全てidleだった。
  その後、依頼元で親だけをSIGINTにより中断し、終了130を確認した。
  したがって今回は「30分のtimeoutまで実際に待った」という記録ではない。
- 根拠: conductorログ3532055行、親ログの受付行151と他3件のcallback行、保存した途中停止記録。

### 考えられる原因

照合した`requeue_with_retry!`の上限超過分岐（509〜514行付近）は、
標準出力とCSVへ破棄を記録してreturnするだけで、親への通知処理がない。
workerはBUSY拒否時にjob IDを発行しないため、通常のworker完了通知も発生しない。

さらに既存の結果通知はworker job IDを中心にしている。親側はconductorの受付IDを知っているが、
worker未受理の失敗ではjob IDが存在しない。この状態も通知・照合できる契約が必要になる。

### 対策案

- conductorが受付済みタスクを最終的に実行しないと決めたら、親がそのtask IDを指定して
  「実行されず終了した」と確定できるようにする。通知、状態照会、または両者の組合せを検討してほしい。
- 受付時のtask ID、終端状態、理由を扱い、worker job IDが未発行でも成立させる。
  通知失敗時の記録・再確認方法、遅れて届く通知、重複通知の扱いも検討対象にする。
- Syncopade固有の一般的な契約として整備し、MDOの独自返却形式やファイル名へ依存させない。
- 発信するだけでなく、公開client側での受信・解釈・元の受付IDとの照合まで確認してほしい。
  依頼元の現行受信処理はworkerの失敗通知を受付IDに結び付けられない場合、未帰属として記録する。
  新しい契約に合わせてMDO側の薄い受信処理を変更する必要があれば、その変更点を知らせてほしい。

確認してほしい小テスト: workerが1回も受理しないまま打ち切られるケースで、受付ID付きの
終了結果を親が把握できること。複数タスク中のどれが終了したかを識別でき、他の成功結果を
取り違えず、通常の長い待機期限まで待たないこと。重複通知で二重に完了計上しないことも確認する。

## 再試験・修正後の引渡し

- 重いSO4/SO5は再現に必須ではない。状態確認と受付の順を制御できる小さなタスクで
  上の3件を個別に確認した後、複数タスクの通常運転を統合確認してほしい。
- 既存のworker排他、BUSY時にjob IDを発行しない挙動、正常な完了通知を壊さない。
- 状態の巻き戻り防止だけでなく、BUSYが実際に返る場合と最終打切りの場合も試す。
  発生頻度が下がっただけで「通知なし破棄」を未検査のまま残さない。
- 修正revision、実稼働conductor/workerへ反映する方法、実施したテストと結果、
  親側に必要なAPI/通信契約の変更を共有してほしい。依頼元側の数値・物理条件の変更は不要。
- こちらのSO6後段接続TODOはStep 8停止を維持する。修正確認・依存反映後に、
  別途再開判断を行う。今回のログのtask IDはconductorで破棄済みと判明したが、
  他の未完タスクや新しい実行の状態まで一括して判断しない。

## 証拠を読むための補足

正本から対象タスクと例外本文を探す読取り専用コマンド:

```sh
rg -n -A 10 '5799dcc7-078f-431d-abe9-f323c5724c82' /Volumes/syncopade_nfs/conductor_events.csv
sed -n '3531994,3532069p' /Volumes/syncopade_nfs/conductor_events.csv
```

親ログと全途中出力:

- このリポジトリの`outputs/so6_downstream_interruptions/so6-downstream-on-20260909-stopped/`。
  `step8-on.log`、`interruption.toml`、`shared_snapshot/`に保存済み。
- 同じ内容を`/Users/endo/Desktop/ManipMDO_results/so6_downstream_interruptions/so6-downstream-on-20260909-stopped/`
  に複製し、857ファイルのhash一致を確認済み。
- この保存一式はconductorログを受け取る前の記録。`cause_confirmed=false`や取消し未確認の
  記録を後から書き換えていない。今回のtask破棄の特定は、本依頼書と後から受け取った正本ログが追加証拠となる。
- 失敗時点までSO4 15件は正常返却（設計条件は未達）。SO5は第4世代17候補を比較済み、
  次世代3候補を追加で受信済み。正式なSO5最終結果・後段接続の完了記録は未発行。

この依頼書の発行で、SyncopadeやMDOの実装、試験条件、受付済みタスクの状態は変更していない。
